# source will be downloaded prior to filling in jinja templates
# Example assumes that this folder has setup.py in it
source:
  git_url: ../

package:
  name: dropbot
{% if GIT_DESCRIBE_NUMBER > '0' %}
  version: {{ GIT_DESCRIBE_TAG[1:] }}.post{{ GIT_DESCRIBE_NUMBER }}
{% else %}
  version: {{ GIT_DESCRIBE_TAG[1:] }}.0
{% endif %}

requirements:
  build:
    - arduino-linked-list >=1.2.3
    - arduino-slow-soft-wire
    - future
    - nanopb-helpers >=0.6
    - paver
    - pip
    - platformio-framework-arduinoteensy >=1.136.0
    - platformio-helpers >=0.3
    - platformio-platform-teensy >=2.0.0
    - platformio-tool-scons
    - platformio-tool-teensy
    - platformio-toolchain-gccarmnoneeabi
    - pytables
    - python 2.7*
    - serial-device >=0.7
    - teensy-minimal-rpc >=0.4
    - base-node-rpc >=0.26

  run:
    - arduino-linked-list >=1.2.3
    - arduino-slow-soft-wire
    - nanopb-helpers >=0.6
    - platformio-helpers >=0.3
    - platformio-tool-scons
    - platformio-tool-teensy
    - protobuf
    - pytables
    - python 2.7*
    - serial-device >=0.7
    - teensy-minimal-rpc >=0.4
    - base-node
    - base-node-rpc >=0.26

test:
  imports:
    - dropbot
    - dropbot.proxy

about:
  home: https://github.com/wheeler-microfluidics/dropbot
  license: MIT
