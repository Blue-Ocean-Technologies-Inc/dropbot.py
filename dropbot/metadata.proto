syntax="proto2";
package metadata;

message Hardware {
  /* # Identity structure #
   *
   * This structure contains fields are stored in
   * persistent memory (i.e., EEPROM) on each piece of hardware/
   * circuit board..
   */
  optional bytes uuid = 1;
  optional string version = 2;
  optional string name = 3;
}
